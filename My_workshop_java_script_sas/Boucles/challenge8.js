const prompt = require("prompt-sync");
const n = prompt = ("enter le nombre n : ");
if (n == 0 || n == 1)
    console.log(n);
else if (n >= 2){
                                                                                                                                                                              
}
